import React, { FC } from 'react';

const Login: FC = () => {
  return (
    <div>
      <button type="button" onClick={() => {}}>
        Login
      </button>
    </div>
  );
};

export default Login;
